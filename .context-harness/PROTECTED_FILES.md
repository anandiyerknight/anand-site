# Protected Files

Treat these as high-risk unless the task explicitly requires them:

- lockfiles
- generated build output
- production data
- secrets and `.env` files
- migrations
- schema files
- deployment config

If editing a protected file, explain why and run the narrowest relevant verification.

