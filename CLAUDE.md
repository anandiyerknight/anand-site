# anand-site — Claude Project Memory

**Status:** LIVE at anandiyer.co.in. Deploy via `git push vercel main` ONLY (anandiyerknight account; `origin` = gutguruswetha-ai mirror — switch gh account before pushing). RESUME: grant Apps Script permissions → E2E test → commit autoOpen + CTA changes.

@docs/CONTEXT.md
